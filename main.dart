import 'dart:io';

void main(){
  int x = 80;
  // prefix, postfixt
  ++x; //prefix increament
  --x; // prefix decremeant
  x++; // postfix increament
  x--; //postfixt decrement
  print(x);

}